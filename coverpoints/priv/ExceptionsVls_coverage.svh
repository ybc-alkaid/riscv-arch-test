///////////////////////////////////////////
//
// RISC-V Architectural Functional Coverage Covergroups
//
// This file is autogenerated by covergroupgen.
// Do not modify directly.
//
// Copyright (C) 2024 Harvey Mudd College, 10x Engineers, UET Lahore, Habib University
//
// SPDX-License-Identifier: Apache-2.0
//
////////////////////////////////////////////////////////////////////////////////////////////////

`define COVER_EXCEPTIONSVLS
`define COVER_EXCEPTIONSVLSCUSTOMEFFEW
`ifdef UDB_ELEN_EFFEW
    `define SEW_EFFEW_EQ_ELEN
`endif
`ifdef UDB_ELEN_TWOEFFEW
    `define SEW_EFFEW_EQ_ELEN_DIV_2
`endif
covergroup ExceptionsVls_vl1re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl1re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl1re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl1re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl2re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl2re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl2re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl2re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl4re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl4re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl4re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl4re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl8re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl8re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl8re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vl8re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vle8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlm_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vloxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vloxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vloxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg2e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg3e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg4e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg5e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg6e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg7e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlseg8e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_ffLS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector fault-first load/store exception operations
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside ffLS)

    mask_disabled: coverpoint ins.current.insn[25] {
        bins disabled = {1'b1};
    }

    rs1_eq_0: coverpoint unsigned'(ins.current.rs1_val) {
        bins zero = {0};
    }

    cp_exceptionsv_ffLS_first_elm_trap : cross std_trap_vec, mask_disabled, rs1_eq_0;

    //// end cp_exceptionsv_ffLS////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vlsseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vluxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vluxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vluxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vs1r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vs2r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vs4r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vs8r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsm_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsoxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsoxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsoxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vssseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew16
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=16 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=16 (ei16 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE16
    index_eew16 : coverpoint ins.current.insn[14:12] {
        bins e16 = {3'b101};
    }
    cp_exceptionsv_index_eew16 : cross std_trap_vec, index_eew16;
    `endif

    //// end cp_exceptionsv_index_eew16//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVls_vsuxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew32
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=32 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=32 (ei32 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE32
    index_eew32 : coverpoint ins.current.insn[14:12] {
        bins e32 = {3'b110};
    }
    cp_exceptionsv_index_eew32 : cross std_trap_vec, index_eew32;
    `endif

    //// end cp_exceptionsv_index_eew32//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup ExceptionsVls_vsuxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_index_eew64
    //////////////////////////////////////////////////////////////////////////////////

    // Cover that an indexed load/store with EEW=64 > MAXINDEXEEW was attempted and trapped.
    // ins.current.insn[14:12] encodes the index EEW (000=8, 101=16, 110=32, 111=64).
    // Assign this coverpoint only to instructions with index EEW=64 (ei64 instructions).
    // Note: std_trap_vec is defined in cp_exceptionsv_LS (always present alongside this).

    `ifndef MAXINDEXEEW_GE64
    index_eew64 : coverpoint ins.current.insn[14:12] {
        bins e64 = {3'b111};
    }
    cp_exceptionsv_index_eew64 : cross std_trap_vec, index_eew64;
    `endif

    //// end cp_exceptionsv_index_eew64//////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup ExceptionsVls_vsuxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_LS
    //////////////////////////////////////////////////////////////////////////////////

    // Custom coverpoints for Vector load/store exception operations

    std_trap_vec : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_LS////////////////////////////////////////////////

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_address_fault
    // //////////////////////////////////////////////////////////////////////////////////////////////////////////

    // Helper: valid vector type (vill=0)
    vtype_valid_20000f: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Main condition: instruction trapped (load/store access or page fault detected via mcause).
    // For indexed instructions with unsupported EEW (exceeds MAXINDEXEEW), the instruction
    // traps at decode with illegal instruction (mcause=2), not during memory access.
    // Accept mcause=2 for these cases. mop field insn[27:26]: 01=indexed-unordered, 11=indexed-ordered.
    trap_occurred_20000f: coverpoint (
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 5 |   // load access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 7 |   // store access fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 13 |  // load page fault
        get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 15    // store page fault
        `ifndef MAXINDEXEEW_GE64
        | (ins.current.insn[27:26] inside {2'b01, 2'b11} &&
           ins.current.insn[14:12] inside {3'b111
               `ifndef MAXINDEXEEW_GE32
               , 3'b110
               `ifndef MAXINDEXEEW_GE16
               , 3'b101
               `endif
               `endif
           } &&
           get_csr_val(ins.hart, ins.issue, `SAMPLE_AFTER, "mcause", "int") == 2)  // illegal instruction
        `endif
    ) {
        bins trapped = {1'b1};
    }

    // Cross: valid vtype AND trap occurred
    cp_exceptionsv_address_fault: cross vtype_valid_20000f, trap_occurred_20000f;

//// end cp_exceptionsv_address_fault ///////////////////////////////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_exceptionsv_indexed
    //////////////////////////////////////////////////////////////////////////////////

    // Records that an indexed vector load/store was executed under otherwise-legal
    // conditions (vill=0, vstart=0, vl>0, mstatus.vs!=0, rs1 aligned to 8 bytes so
    // no element is misaligned for any supported SEW). Because every other
    // precondition is valid, any trap observed for this instruction is attributable
    // to MAXINDEXEEW gating the index EEW.

    cp_exceptionsv_indexed : coverpoint {get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") == 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") != 0 &
                        get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") != 0 &
                        ins.current.rs1_val[2:0] == 3'b000
                    }
    {
        bins true = {1'b1};
    }

    //// end cp_exceptionsv_indexed //////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
function void exceptionsvls_sample(int hart, int issue, ins_t ins);

    case (traceDataQ[hart][issue][0].inst_name)
        "vl1re16.v"     : begin
            ExceptionsVls_vl1re16_v_cg.sample(ins);
        end
        "vl1re32.v"     : begin
            ExceptionsVls_vl1re32_v_cg.sample(ins);
        end
        "vl1re64.v"     : begin
            ExceptionsVls_vl1re64_v_cg.sample(ins);
        end
        "vl1re8.v"     : begin
            ExceptionsVls_vl1re8_v_cg.sample(ins);
        end
        "vl2re16.v"     : begin
            ExceptionsVls_vl2re16_v_cg.sample(ins);
        end
        "vl2re32.v"     : begin
            ExceptionsVls_vl2re32_v_cg.sample(ins);
        end
        "vl2re64.v"     : begin
            ExceptionsVls_vl2re64_v_cg.sample(ins);
        end
        "vl2re8.v"     : begin
            ExceptionsVls_vl2re8_v_cg.sample(ins);
        end
        "vl4re16.v"     : begin
            ExceptionsVls_vl4re16_v_cg.sample(ins);
        end
        "vl4re32.v"     : begin
            ExceptionsVls_vl4re32_v_cg.sample(ins);
        end
        "vl4re64.v"     : begin
            ExceptionsVls_vl4re64_v_cg.sample(ins);
        end
        "vl4re8.v"     : begin
            ExceptionsVls_vl4re8_v_cg.sample(ins);
        end
        "vl8re16.v"     : begin
            ExceptionsVls_vl8re16_v_cg.sample(ins);
        end
        "vl8re32.v"     : begin
            ExceptionsVls_vl8re32_v_cg.sample(ins);
        end
        "vl8re64.v"     : begin
            ExceptionsVls_vl8re64_v_cg.sample(ins);
        end
        "vl8re8.v"     : begin
            ExceptionsVls_vl8re8_v_cg.sample(ins);
        end
        "vle16.v"     : begin
            ExceptionsVls_vle16_v_cg.sample(ins);
        end
        "vle16ff.v"     : begin
            ExceptionsVls_vle16ff_v_cg.sample(ins);
        end
        "vle32.v"     : begin
            ExceptionsVls_vle32_v_cg.sample(ins);
        end
        "vle32ff.v"     : begin
            ExceptionsVls_vle32ff_v_cg.sample(ins);
        end
        "vle64.v"     : begin
            ExceptionsVls_vle64_v_cg.sample(ins);
        end
        "vle64ff.v"     : begin
            ExceptionsVls_vle64ff_v_cg.sample(ins);
        end
        "vle8.v"     : begin
            ExceptionsVls_vle8_v_cg.sample(ins);
        end
        "vle8ff.v"     : begin
            ExceptionsVls_vle8ff_v_cg.sample(ins);
        end
        "vlm.v"     : begin
            ExceptionsVls_vlm_v_cg.sample(ins);
        end
        "vloxei16.v"     : begin
            ExceptionsVls_vloxei16_v_cg.sample(ins);
        end
        "vloxei32.v"     : begin
            ExceptionsVls_vloxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxei64.v"     : begin
            ExceptionsVls_vloxei64_v_cg.sample(ins);
        end
`endif
        "vloxei8.v"     : begin
            ExceptionsVls_vloxei8_v_cg.sample(ins);
        end
        "vloxseg2ei16.v"     : begin
            ExceptionsVls_vloxseg2ei16_v_cg.sample(ins);
        end
        "vloxseg2ei32.v"     : begin
            ExceptionsVls_vloxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg2ei64.v"     : begin
            ExceptionsVls_vloxseg2ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg2ei8.v"     : begin
            ExceptionsVls_vloxseg2ei8_v_cg.sample(ins);
        end
        "vloxseg3ei16.v"     : begin
            ExceptionsVls_vloxseg3ei16_v_cg.sample(ins);
        end
        "vloxseg3ei32.v"     : begin
            ExceptionsVls_vloxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg3ei64.v"     : begin
            ExceptionsVls_vloxseg3ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg3ei8.v"     : begin
            ExceptionsVls_vloxseg3ei8_v_cg.sample(ins);
        end
        "vloxseg4ei16.v"     : begin
            ExceptionsVls_vloxseg4ei16_v_cg.sample(ins);
        end
        "vloxseg4ei32.v"     : begin
            ExceptionsVls_vloxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg4ei64.v"     : begin
            ExceptionsVls_vloxseg4ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg4ei8.v"     : begin
            ExceptionsVls_vloxseg4ei8_v_cg.sample(ins);
        end
        "vloxseg5ei16.v"     : begin
            ExceptionsVls_vloxseg5ei16_v_cg.sample(ins);
        end
        "vloxseg5ei32.v"     : begin
            ExceptionsVls_vloxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg5ei64.v"     : begin
            ExceptionsVls_vloxseg5ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg5ei8.v"     : begin
            ExceptionsVls_vloxseg5ei8_v_cg.sample(ins);
        end
        "vloxseg6ei16.v"     : begin
            ExceptionsVls_vloxseg6ei16_v_cg.sample(ins);
        end
        "vloxseg6ei32.v"     : begin
            ExceptionsVls_vloxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg6ei64.v"     : begin
            ExceptionsVls_vloxseg6ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg6ei8.v"     : begin
            ExceptionsVls_vloxseg6ei8_v_cg.sample(ins);
        end
        "vloxseg7ei16.v"     : begin
            ExceptionsVls_vloxseg7ei16_v_cg.sample(ins);
        end
        "vloxseg7ei32.v"     : begin
            ExceptionsVls_vloxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg7ei64.v"     : begin
            ExceptionsVls_vloxseg7ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg7ei8.v"     : begin
            ExceptionsVls_vloxseg7ei8_v_cg.sample(ins);
        end
        "vloxseg8ei16.v"     : begin
            ExceptionsVls_vloxseg8ei16_v_cg.sample(ins);
        end
        "vloxseg8ei32.v"     : begin
            ExceptionsVls_vloxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vloxseg8ei64.v"     : begin
            ExceptionsVls_vloxseg8ei64_v_cg.sample(ins);
        end
`endif
        "vloxseg8ei8.v"     : begin
            ExceptionsVls_vloxseg8ei8_v_cg.sample(ins);
        end
        "vlse16.v"     : begin
            ExceptionsVls_vlse16_v_cg.sample(ins);
        end
        "vlse32.v"     : begin
            ExceptionsVls_vlse32_v_cg.sample(ins);
        end
        "vlse64.v"     : begin
            ExceptionsVls_vlse64_v_cg.sample(ins);
        end
        "vlse8.v"     : begin
            ExceptionsVls_vlse8_v_cg.sample(ins);
        end
        "vlseg2e16.v"     : begin
            ExceptionsVls_vlseg2e16_v_cg.sample(ins);
        end
        "vlseg2e16ff.v"     : begin
            ExceptionsVls_vlseg2e16ff_v_cg.sample(ins);
        end
        "vlseg2e32.v"     : begin
            ExceptionsVls_vlseg2e32_v_cg.sample(ins);
        end
        "vlseg2e32ff.v"     : begin
            ExceptionsVls_vlseg2e32ff_v_cg.sample(ins);
        end
        "vlseg2e64.v"     : begin
            ExceptionsVls_vlseg2e64_v_cg.sample(ins);
        end
        "vlseg2e64ff.v"     : begin
            ExceptionsVls_vlseg2e64ff_v_cg.sample(ins);
        end
        "vlseg2e8.v"     : begin
            ExceptionsVls_vlseg2e8_v_cg.sample(ins);
        end
        "vlseg2e8ff.v"     : begin
            ExceptionsVls_vlseg2e8ff_v_cg.sample(ins);
        end
        "vlseg3e16.v"     : begin
            ExceptionsVls_vlseg3e16_v_cg.sample(ins);
        end
        "vlseg3e16ff.v"     : begin
            ExceptionsVls_vlseg3e16ff_v_cg.sample(ins);
        end
        "vlseg3e32.v"     : begin
            ExceptionsVls_vlseg3e32_v_cg.sample(ins);
        end
        "vlseg3e32ff.v"     : begin
            ExceptionsVls_vlseg3e32ff_v_cg.sample(ins);
        end
        "vlseg3e64.v"     : begin
            ExceptionsVls_vlseg3e64_v_cg.sample(ins);
        end
        "vlseg3e64ff.v"     : begin
            ExceptionsVls_vlseg3e64ff_v_cg.sample(ins);
        end
        "vlseg3e8.v"     : begin
            ExceptionsVls_vlseg3e8_v_cg.sample(ins);
        end
        "vlseg3e8ff.v"     : begin
            ExceptionsVls_vlseg3e8ff_v_cg.sample(ins);
        end
        "vlseg4e16.v"     : begin
            ExceptionsVls_vlseg4e16_v_cg.sample(ins);
        end
        "vlseg4e16ff.v"     : begin
            ExceptionsVls_vlseg4e16ff_v_cg.sample(ins);
        end
        "vlseg4e32.v"     : begin
            ExceptionsVls_vlseg4e32_v_cg.sample(ins);
        end
        "vlseg4e32ff.v"     : begin
            ExceptionsVls_vlseg4e32ff_v_cg.sample(ins);
        end
        "vlseg4e64.v"     : begin
            ExceptionsVls_vlseg4e64_v_cg.sample(ins);
        end
        "vlseg4e64ff.v"     : begin
            ExceptionsVls_vlseg4e64ff_v_cg.sample(ins);
        end
        "vlseg4e8.v"     : begin
            ExceptionsVls_vlseg4e8_v_cg.sample(ins);
        end
        "vlseg4e8ff.v"     : begin
            ExceptionsVls_vlseg4e8ff_v_cg.sample(ins);
        end
        "vlseg5e16.v"     : begin
            ExceptionsVls_vlseg5e16_v_cg.sample(ins);
        end
        "vlseg5e16ff.v"     : begin
            ExceptionsVls_vlseg5e16ff_v_cg.sample(ins);
        end
        "vlseg5e32.v"     : begin
            ExceptionsVls_vlseg5e32_v_cg.sample(ins);
        end
        "vlseg5e32ff.v"     : begin
            ExceptionsVls_vlseg5e32ff_v_cg.sample(ins);
        end
        "vlseg5e64.v"     : begin
            ExceptionsVls_vlseg5e64_v_cg.sample(ins);
        end
        "vlseg5e64ff.v"     : begin
            ExceptionsVls_vlseg5e64ff_v_cg.sample(ins);
        end
        "vlseg5e8.v"     : begin
            ExceptionsVls_vlseg5e8_v_cg.sample(ins);
        end
        "vlseg5e8ff.v"     : begin
            ExceptionsVls_vlseg5e8ff_v_cg.sample(ins);
        end
        "vlseg6e16.v"     : begin
            ExceptionsVls_vlseg6e16_v_cg.sample(ins);
        end
        "vlseg6e16ff.v"     : begin
            ExceptionsVls_vlseg6e16ff_v_cg.sample(ins);
        end
        "vlseg6e32.v"     : begin
            ExceptionsVls_vlseg6e32_v_cg.sample(ins);
        end
        "vlseg6e32ff.v"     : begin
            ExceptionsVls_vlseg6e32ff_v_cg.sample(ins);
        end
        "vlseg6e64.v"     : begin
            ExceptionsVls_vlseg6e64_v_cg.sample(ins);
        end
        "vlseg6e64ff.v"     : begin
            ExceptionsVls_vlseg6e64ff_v_cg.sample(ins);
        end
        "vlseg6e8.v"     : begin
            ExceptionsVls_vlseg6e8_v_cg.sample(ins);
        end
        "vlseg6e8ff.v"     : begin
            ExceptionsVls_vlseg6e8ff_v_cg.sample(ins);
        end
        "vlseg7e16.v"     : begin
            ExceptionsVls_vlseg7e16_v_cg.sample(ins);
        end
        "vlseg7e16ff.v"     : begin
            ExceptionsVls_vlseg7e16ff_v_cg.sample(ins);
        end
        "vlseg7e32.v"     : begin
            ExceptionsVls_vlseg7e32_v_cg.sample(ins);
        end
        "vlseg7e32ff.v"     : begin
            ExceptionsVls_vlseg7e32ff_v_cg.sample(ins);
        end
        "vlseg7e64.v"     : begin
            ExceptionsVls_vlseg7e64_v_cg.sample(ins);
        end
        "vlseg7e64ff.v"     : begin
            ExceptionsVls_vlseg7e64ff_v_cg.sample(ins);
        end
        "vlseg7e8.v"     : begin
            ExceptionsVls_vlseg7e8_v_cg.sample(ins);
        end
        "vlseg7e8ff.v"     : begin
            ExceptionsVls_vlseg7e8ff_v_cg.sample(ins);
        end
        "vlseg8e16.v"     : begin
            ExceptionsVls_vlseg8e16_v_cg.sample(ins);
        end
        "vlseg8e16ff.v"     : begin
            ExceptionsVls_vlseg8e16ff_v_cg.sample(ins);
        end
        "vlseg8e32.v"     : begin
            ExceptionsVls_vlseg8e32_v_cg.sample(ins);
        end
        "vlseg8e32ff.v"     : begin
            ExceptionsVls_vlseg8e32ff_v_cg.sample(ins);
        end
        "vlseg8e64.v"     : begin
            ExceptionsVls_vlseg8e64_v_cg.sample(ins);
        end
        "vlseg8e64ff.v"     : begin
            ExceptionsVls_vlseg8e64ff_v_cg.sample(ins);
        end
        "vlseg8e8.v"     : begin
            ExceptionsVls_vlseg8e8_v_cg.sample(ins);
        end
        "vlseg8e8ff.v"     : begin
            ExceptionsVls_vlseg8e8ff_v_cg.sample(ins);
        end
        "vlsseg2e16.v"     : begin
            ExceptionsVls_vlsseg2e16_v_cg.sample(ins);
        end
        "vlsseg2e32.v"     : begin
            ExceptionsVls_vlsseg2e32_v_cg.sample(ins);
        end
        "vlsseg2e64.v"     : begin
            ExceptionsVls_vlsseg2e64_v_cg.sample(ins);
        end
        "vlsseg2e8.v"     : begin
            ExceptionsVls_vlsseg2e8_v_cg.sample(ins);
        end
        "vlsseg3e16.v"     : begin
            ExceptionsVls_vlsseg3e16_v_cg.sample(ins);
        end
        "vlsseg3e32.v"     : begin
            ExceptionsVls_vlsseg3e32_v_cg.sample(ins);
        end
        "vlsseg3e64.v"     : begin
            ExceptionsVls_vlsseg3e64_v_cg.sample(ins);
        end
        "vlsseg3e8.v"     : begin
            ExceptionsVls_vlsseg3e8_v_cg.sample(ins);
        end
        "vlsseg4e16.v"     : begin
            ExceptionsVls_vlsseg4e16_v_cg.sample(ins);
        end
        "vlsseg4e32.v"     : begin
            ExceptionsVls_vlsseg4e32_v_cg.sample(ins);
        end
        "vlsseg4e64.v"     : begin
            ExceptionsVls_vlsseg4e64_v_cg.sample(ins);
        end
        "vlsseg4e8.v"     : begin
            ExceptionsVls_vlsseg4e8_v_cg.sample(ins);
        end
        "vlsseg5e16.v"     : begin
            ExceptionsVls_vlsseg5e16_v_cg.sample(ins);
        end
        "vlsseg5e32.v"     : begin
            ExceptionsVls_vlsseg5e32_v_cg.sample(ins);
        end
        "vlsseg5e64.v"     : begin
            ExceptionsVls_vlsseg5e64_v_cg.sample(ins);
        end
        "vlsseg5e8.v"     : begin
            ExceptionsVls_vlsseg5e8_v_cg.sample(ins);
        end
        "vlsseg6e16.v"     : begin
            ExceptionsVls_vlsseg6e16_v_cg.sample(ins);
        end
        "vlsseg6e32.v"     : begin
            ExceptionsVls_vlsseg6e32_v_cg.sample(ins);
        end
        "vlsseg6e64.v"     : begin
            ExceptionsVls_vlsseg6e64_v_cg.sample(ins);
        end
        "vlsseg6e8.v"     : begin
            ExceptionsVls_vlsseg6e8_v_cg.sample(ins);
        end
        "vlsseg7e16.v"     : begin
            ExceptionsVls_vlsseg7e16_v_cg.sample(ins);
        end
        "vlsseg7e32.v"     : begin
            ExceptionsVls_vlsseg7e32_v_cg.sample(ins);
        end
        "vlsseg7e64.v"     : begin
            ExceptionsVls_vlsseg7e64_v_cg.sample(ins);
        end
        "vlsseg7e8.v"     : begin
            ExceptionsVls_vlsseg7e8_v_cg.sample(ins);
        end
        "vlsseg8e16.v"     : begin
            ExceptionsVls_vlsseg8e16_v_cg.sample(ins);
        end
        "vlsseg8e32.v"     : begin
            ExceptionsVls_vlsseg8e32_v_cg.sample(ins);
        end
        "vlsseg8e64.v"     : begin
            ExceptionsVls_vlsseg8e64_v_cg.sample(ins);
        end
        "vlsseg8e8.v"     : begin
            ExceptionsVls_vlsseg8e8_v_cg.sample(ins);
        end
        "vluxei16.v"     : begin
            ExceptionsVls_vluxei16_v_cg.sample(ins);
        end
        "vluxei32.v"     : begin
            ExceptionsVls_vluxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxei64.v"     : begin
            ExceptionsVls_vluxei64_v_cg.sample(ins);
        end
`endif
        "vluxei8.v"     : begin
            ExceptionsVls_vluxei8_v_cg.sample(ins);
        end
        "vluxseg2ei16.v"     : begin
            ExceptionsVls_vluxseg2ei16_v_cg.sample(ins);
        end
        "vluxseg2ei32.v"     : begin
            ExceptionsVls_vluxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg2ei64.v"     : begin
            ExceptionsVls_vluxseg2ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg2ei8.v"     : begin
            ExceptionsVls_vluxseg2ei8_v_cg.sample(ins);
        end
        "vluxseg3ei16.v"     : begin
            ExceptionsVls_vluxseg3ei16_v_cg.sample(ins);
        end
        "vluxseg3ei32.v"     : begin
            ExceptionsVls_vluxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg3ei64.v"     : begin
            ExceptionsVls_vluxseg3ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg3ei8.v"     : begin
            ExceptionsVls_vluxseg3ei8_v_cg.sample(ins);
        end
        "vluxseg4ei16.v"     : begin
            ExceptionsVls_vluxseg4ei16_v_cg.sample(ins);
        end
        "vluxseg4ei32.v"     : begin
            ExceptionsVls_vluxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg4ei64.v"     : begin
            ExceptionsVls_vluxseg4ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg4ei8.v"     : begin
            ExceptionsVls_vluxseg4ei8_v_cg.sample(ins);
        end
        "vluxseg5ei16.v"     : begin
            ExceptionsVls_vluxseg5ei16_v_cg.sample(ins);
        end
        "vluxseg5ei32.v"     : begin
            ExceptionsVls_vluxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg5ei64.v"     : begin
            ExceptionsVls_vluxseg5ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg5ei8.v"     : begin
            ExceptionsVls_vluxseg5ei8_v_cg.sample(ins);
        end
        "vluxseg6ei16.v"     : begin
            ExceptionsVls_vluxseg6ei16_v_cg.sample(ins);
        end
        "vluxseg6ei32.v"     : begin
            ExceptionsVls_vluxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg6ei64.v"     : begin
            ExceptionsVls_vluxseg6ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg6ei8.v"     : begin
            ExceptionsVls_vluxseg6ei8_v_cg.sample(ins);
        end
        "vluxseg7ei16.v"     : begin
            ExceptionsVls_vluxseg7ei16_v_cg.sample(ins);
        end
        "vluxseg7ei32.v"     : begin
            ExceptionsVls_vluxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg7ei64.v"     : begin
            ExceptionsVls_vluxseg7ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg7ei8.v"     : begin
            ExceptionsVls_vluxseg7ei8_v_cg.sample(ins);
        end
        "vluxseg8ei16.v"     : begin
            ExceptionsVls_vluxseg8ei16_v_cg.sample(ins);
        end
        "vluxseg8ei32.v"     : begin
            ExceptionsVls_vluxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vluxseg8ei64.v"     : begin
            ExceptionsVls_vluxseg8ei64_v_cg.sample(ins);
        end
`endif
        "vluxseg8ei8.v"     : begin
            ExceptionsVls_vluxseg8ei8_v_cg.sample(ins);
        end
        "vs1r.v"     : begin
            ExceptionsVls_vs1r_v_cg.sample(ins);
        end
        "vs2r.v"     : begin
            ExceptionsVls_vs2r_v_cg.sample(ins);
        end
        "vs4r.v"     : begin
            ExceptionsVls_vs4r_v_cg.sample(ins);
        end
        "vs8r.v"     : begin
            ExceptionsVls_vs8r_v_cg.sample(ins);
        end
        "vse16.v"     : begin
            ExceptionsVls_vse16_v_cg.sample(ins);
        end
        "vse32.v"     : begin
            ExceptionsVls_vse32_v_cg.sample(ins);
        end
        "vse64.v"     : begin
            ExceptionsVls_vse64_v_cg.sample(ins);
        end
        "vse8.v"     : begin
            ExceptionsVls_vse8_v_cg.sample(ins);
        end
        "vsm.v"     : begin
            ExceptionsVls_vsm_v_cg.sample(ins);
        end
        "vsoxei16.v"     : begin
            ExceptionsVls_vsoxei16_v_cg.sample(ins);
        end
        "vsoxei32.v"     : begin
            ExceptionsVls_vsoxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxei64.v"     : begin
            ExceptionsVls_vsoxei64_v_cg.sample(ins);
        end
`endif
        "vsoxei8.v"     : begin
            ExceptionsVls_vsoxei8_v_cg.sample(ins);
        end
        "vsoxseg2ei16.v"     : begin
            ExceptionsVls_vsoxseg2ei16_v_cg.sample(ins);
        end
        "vsoxseg2ei32.v"     : begin
            ExceptionsVls_vsoxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg2ei64.v"     : begin
            ExceptionsVls_vsoxseg2ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg2ei8.v"     : begin
            ExceptionsVls_vsoxseg2ei8_v_cg.sample(ins);
        end
        "vsoxseg3ei16.v"     : begin
            ExceptionsVls_vsoxseg3ei16_v_cg.sample(ins);
        end
        "vsoxseg3ei32.v"     : begin
            ExceptionsVls_vsoxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg3ei64.v"     : begin
            ExceptionsVls_vsoxseg3ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg3ei8.v"     : begin
            ExceptionsVls_vsoxseg3ei8_v_cg.sample(ins);
        end
        "vsoxseg4ei16.v"     : begin
            ExceptionsVls_vsoxseg4ei16_v_cg.sample(ins);
        end
        "vsoxseg4ei32.v"     : begin
            ExceptionsVls_vsoxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg4ei64.v"     : begin
            ExceptionsVls_vsoxseg4ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg4ei8.v"     : begin
            ExceptionsVls_vsoxseg4ei8_v_cg.sample(ins);
        end
        "vsoxseg5ei16.v"     : begin
            ExceptionsVls_vsoxseg5ei16_v_cg.sample(ins);
        end
        "vsoxseg5ei32.v"     : begin
            ExceptionsVls_vsoxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg5ei64.v"     : begin
            ExceptionsVls_vsoxseg5ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg5ei8.v"     : begin
            ExceptionsVls_vsoxseg5ei8_v_cg.sample(ins);
        end
        "vsoxseg6ei16.v"     : begin
            ExceptionsVls_vsoxseg6ei16_v_cg.sample(ins);
        end
        "vsoxseg6ei32.v"     : begin
            ExceptionsVls_vsoxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg6ei64.v"     : begin
            ExceptionsVls_vsoxseg6ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg6ei8.v"     : begin
            ExceptionsVls_vsoxseg6ei8_v_cg.sample(ins);
        end
        "vsoxseg7ei16.v"     : begin
            ExceptionsVls_vsoxseg7ei16_v_cg.sample(ins);
        end
        "vsoxseg7ei32.v"     : begin
            ExceptionsVls_vsoxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg7ei64.v"     : begin
            ExceptionsVls_vsoxseg7ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg7ei8.v"     : begin
            ExceptionsVls_vsoxseg7ei8_v_cg.sample(ins);
        end
        "vsoxseg8ei16.v"     : begin
            ExceptionsVls_vsoxseg8ei16_v_cg.sample(ins);
        end
        "vsoxseg8ei32.v"     : begin
            ExceptionsVls_vsoxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsoxseg8ei64.v"     : begin
            ExceptionsVls_vsoxseg8ei64_v_cg.sample(ins);
        end
`endif
        "vsoxseg8ei8.v"     : begin
            ExceptionsVls_vsoxseg8ei8_v_cg.sample(ins);
        end
        "vsse16.v"     : begin
            ExceptionsVls_vsse16_v_cg.sample(ins);
        end
        "vsse32.v"     : begin
            ExceptionsVls_vsse32_v_cg.sample(ins);
        end
        "vsse64.v"     : begin
            ExceptionsVls_vsse64_v_cg.sample(ins);
        end
        "vsse8.v"     : begin
            ExceptionsVls_vsse8_v_cg.sample(ins);
        end
        "vsseg2e16.v"     : begin
            ExceptionsVls_vsseg2e16_v_cg.sample(ins);
        end
        "vsseg2e32.v"     : begin
            ExceptionsVls_vsseg2e32_v_cg.sample(ins);
        end
        "vsseg2e64.v"     : begin
            ExceptionsVls_vsseg2e64_v_cg.sample(ins);
        end
        "vsseg2e8.v"     : begin
            ExceptionsVls_vsseg2e8_v_cg.sample(ins);
        end
        "vsseg3e16.v"     : begin
            ExceptionsVls_vsseg3e16_v_cg.sample(ins);
        end
        "vsseg3e32.v"     : begin
            ExceptionsVls_vsseg3e32_v_cg.sample(ins);
        end
        "vsseg3e64.v"     : begin
            ExceptionsVls_vsseg3e64_v_cg.sample(ins);
        end
        "vsseg3e8.v"     : begin
            ExceptionsVls_vsseg3e8_v_cg.sample(ins);
        end
        "vsseg4e16.v"     : begin
            ExceptionsVls_vsseg4e16_v_cg.sample(ins);
        end
        "vsseg4e32.v"     : begin
            ExceptionsVls_vsseg4e32_v_cg.sample(ins);
        end
        "vsseg4e64.v"     : begin
            ExceptionsVls_vsseg4e64_v_cg.sample(ins);
        end
        "vsseg4e8.v"     : begin
            ExceptionsVls_vsseg4e8_v_cg.sample(ins);
        end
        "vsseg5e16.v"     : begin
            ExceptionsVls_vsseg5e16_v_cg.sample(ins);
        end
        "vsseg5e32.v"     : begin
            ExceptionsVls_vsseg5e32_v_cg.sample(ins);
        end
        "vsseg5e64.v"     : begin
            ExceptionsVls_vsseg5e64_v_cg.sample(ins);
        end
        "vsseg5e8.v"     : begin
            ExceptionsVls_vsseg5e8_v_cg.sample(ins);
        end
        "vsseg6e16.v"     : begin
            ExceptionsVls_vsseg6e16_v_cg.sample(ins);
        end
        "vsseg6e32.v"     : begin
            ExceptionsVls_vsseg6e32_v_cg.sample(ins);
        end
        "vsseg6e64.v"     : begin
            ExceptionsVls_vsseg6e64_v_cg.sample(ins);
        end
        "vsseg6e8.v"     : begin
            ExceptionsVls_vsseg6e8_v_cg.sample(ins);
        end
        "vsseg7e16.v"     : begin
            ExceptionsVls_vsseg7e16_v_cg.sample(ins);
        end
        "vsseg7e32.v"     : begin
            ExceptionsVls_vsseg7e32_v_cg.sample(ins);
        end
        "vsseg7e64.v"     : begin
            ExceptionsVls_vsseg7e64_v_cg.sample(ins);
        end
        "vsseg7e8.v"     : begin
            ExceptionsVls_vsseg7e8_v_cg.sample(ins);
        end
        "vsseg8e16.v"     : begin
            ExceptionsVls_vsseg8e16_v_cg.sample(ins);
        end
        "vsseg8e32.v"     : begin
            ExceptionsVls_vsseg8e32_v_cg.sample(ins);
        end
        "vsseg8e64.v"     : begin
            ExceptionsVls_vsseg8e64_v_cg.sample(ins);
        end
        "vsseg8e8.v"     : begin
            ExceptionsVls_vsseg8e8_v_cg.sample(ins);
        end
        "vssseg2e16.v"     : begin
            ExceptionsVls_vssseg2e16_v_cg.sample(ins);
        end
        "vssseg2e32.v"     : begin
            ExceptionsVls_vssseg2e32_v_cg.sample(ins);
        end
        "vssseg2e64.v"     : begin
            ExceptionsVls_vssseg2e64_v_cg.sample(ins);
        end
        "vssseg2e8.v"     : begin
            ExceptionsVls_vssseg2e8_v_cg.sample(ins);
        end
        "vssseg3e16.v"     : begin
            ExceptionsVls_vssseg3e16_v_cg.sample(ins);
        end
        "vssseg3e32.v"     : begin
            ExceptionsVls_vssseg3e32_v_cg.sample(ins);
        end
        "vssseg3e64.v"     : begin
            ExceptionsVls_vssseg3e64_v_cg.sample(ins);
        end
        "vssseg3e8.v"     : begin
            ExceptionsVls_vssseg3e8_v_cg.sample(ins);
        end
        "vssseg4e16.v"     : begin
            ExceptionsVls_vssseg4e16_v_cg.sample(ins);
        end
        "vssseg4e32.v"     : begin
            ExceptionsVls_vssseg4e32_v_cg.sample(ins);
        end
        "vssseg4e64.v"     : begin
            ExceptionsVls_vssseg4e64_v_cg.sample(ins);
        end
        "vssseg4e8.v"     : begin
            ExceptionsVls_vssseg4e8_v_cg.sample(ins);
        end
        "vssseg5e16.v"     : begin
            ExceptionsVls_vssseg5e16_v_cg.sample(ins);
        end
        "vssseg5e32.v"     : begin
            ExceptionsVls_vssseg5e32_v_cg.sample(ins);
        end
        "vssseg5e64.v"     : begin
            ExceptionsVls_vssseg5e64_v_cg.sample(ins);
        end
        "vssseg5e8.v"     : begin
            ExceptionsVls_vssseg5e8_v_cg.sample(ins);
        end
        "vssseg6e16.v"     : begin
            ExceptionsVls_vssseg6e16_v_cg.sample(ins);
        end
        "vssseg6e32.v"     : begin
            ExceptionsVls_vssseg6e32_v_cg.sample(ins);
        end
        "vssseg6e64.v"     : begin
            ExceptionsVls_vssseg6e64_v_cg.sample(ins);
        end
        "vssseg6e8.v"     : begin
            ExceptionsVls_vssseg6e8_v_cg.sample(ins);
        end
        "vssseg7e16.v"     : begin
            ExceptionsVls_vssseg7e16_v_cg.sample(ins);
        end
        "vssseg7e32.v"     : begin
            ExceptionsVls_vssseg7e32_v_cg.sample(ins);
        end
        "vssseg7e64.v"     : begin
            ExceptionsVls_vssseg7e64_v_cg.sample(ins);
        end
        "vssseg7e8.v"     : begin
            ExceptionsVls_vssseg7e8_v_cg.sample(ins);
        end
        "vssseg8e16.v"     : begin
            ExceptionsVls_vssseg8e16_v_cg.sample(ins);
        end
        "vssseg8e32.v"     : begin
            ExceptionsVls_vssseg8e32_v_cg.sample(ins);
        end
        "vssseg8e64.v"     : begin
            ExceptionsVls_vssseg8e64_v_cg.sample(ins);
        end
        "vssseg8e8.v"     : begin
            ExceptionsVls_vssseg8e8_v_cg.sample(ins);
        end
        "vsuxei16.v"     : begin
            ExceptionsVls_vsuxei16_v_cg.sample(ins);
        end
        "vsuxei32.v"     : begin
            ExceptionsVls_vsuxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxei64.v"     : begin
            ExceptionsVls_vsuxei64_v_cg.sample(ins);
        end
`endif
        "vsuxei8.v"     : begin
            ExceptionsVls_vsuxei8_v_cg.sample(ins);
        end
        "vsuxseg2ei16.v"     : begin
            ExceptionsVls_vsuxseg2ei16_v_cg.sample(ins);
        end
        "vsuxseg2ei32.v"     : begin
            ExceptionsVls_vsuxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg2ei64.v"     : begin
            ExceptionsVls_vsuxseg2ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg2ei8.v"     : begin
            ExceptionsVls_vsuxseg2ei8_v_cg.sample(ins);
        end
        "vsuxseg3ei16.v"     : begin
            ExceptionsVls_vsuxseg3ei16_v_cg.sample(ins);
        end
        "vsuxseg3ei32.v"     : begin
            ExceptionsVls_vsuxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg3ei64.v"     : begin
            ExceptionsVls_vsuxseg3ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg3ei8.v"     : begin
            ExceptionsVls_vsuxseg3ei8_v_cg.sample(ins);
        end
        "vsuxseg4ei16.v"     : begin
            ExceptionsVls_vsuxseg4ei16_v_cg.sample(ins);
        end
        "vsuxseg4ei32.v"     : begin
            ExceptionsVls_vsuxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg4ei64.v"     : begin
            ExceptionsVls_vsuxseg4ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg4ei8.v"     : begin
            ExceptionsVls_vsuxseg4ei8_v_cg.sample(ins);
        end
        "vsuxseg5ei16.v"     : begin
            ExceptionsVls_vsuxseg5ei16_v_cg.sample(ins);
        end
        "vsuxseg5ei32.v"     : begin
            ExceptionsVls_vsuxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg5ei64.v"     : begin
            ExceptionsVls_vsuxseg5ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg5ei8.v"     : begin
            ExceptionsVls_vsuxseg5ei8_v_cg.sample(ins);
        end
        "vsuxseg6ei16.v"     : begin
            ExceptionsVls_vsuxseg6ei16_v_cg.sample(ins);
        end
        "vsuxseg6ei32.v"     : begin
            ExceptionsVls_vsuxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg6ei64.v"     : begin
            ExceptionsVls_vsuxseg6ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg6ei8.v"     : begin
            ExceptionsVls_vsuxseg6ei8_v_cg.sample(ins);
        end
        "vsuxseg7ei16.v"     : begin
            ExceptionsVls_vsuxseg7ei16_v_cg.sample(ins);
        end
        "vsuxseg7ei32.v"     : begin
            ExceptionsVls_vsuxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg7ei64.v"     : begin
            ExceptionsVls_vsuxseg7ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg7ei8.v"     : begin
            ExceptionsVls_vsuxseg7ei8_v_cg.sample(ins);
        end
        "vsuxseg8ei16.v"     : begin
            ExceptionsVls_vsuxseg8ei16_v_cg.sample(ins);
        end
        "vsuxseg8ei32.v"     : begin
            ExceptionsVls_vsuxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        "vsuxseg8ei64.v"     : begin
            ExceptionsVls_vsuxseg8ei64_v_cg.sample(ins);
        end
`endif
        "vsuxseg8ei8.v"     : begin
            ExceptionsVls_vsuxseg8ei8_v_cg.sample(ins);
        end
    endcase
endfunction
