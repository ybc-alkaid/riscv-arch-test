///////////////////////////////////////////
//
// RISC-V Architectural Functional Coverage Covergroups
//
// This file is autogenerated by covergroupgen.
// Do not modify directly.
//
// Copyright (C) 2024 Harvey Mudd College, 10x Engineers, UET Lahore, Habib University
//
// SPDX-License-Identifier: Apache-2.0
//
////////////////////////////////////////////////////////////////////////////////////////////////

`define COVER_EXCEPTIONSVF64
`define COVER_EXCEPTIONSVFCUSTOM64
`ifdef UDB_ELEN_64
    `define SEW_64_EQ_ELEN
`endif
`ifdef UDB_ELEN_128
    `define SEW_64_EQ_ELEN_DIV_2
`endif
covergroup ExceptionsVf64_vfadd_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfadd_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfclass_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_f_x_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_f_xu_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_rtz_x_f_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_rtz_xu_f_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_x_f_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfcvt_xu_f_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfdiv_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfdiv_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmacc_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmacc_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmadd_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmadd_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmax_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmax_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmerge_vfm_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmin_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmin_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmsac_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmsac_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmsub_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmsub_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmul_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmul_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmv_f_s_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmv_s_f_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfmv_v_f_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmacc_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmacc_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmadd_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmadd_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmsac_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmsac_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmsub_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfnmsub_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfrdiv_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfrec7_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfredmax_vs_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfredmin_vs_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfredosum_vs_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfredusum_vs_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfrsqrt7_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfrsub_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnj_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnj_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnjn_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnjn_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnjx_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsgnjx_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfslide1down_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfslide1up_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsqrt_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsub_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vfsub_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfeq_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfeq_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfge_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfgt_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfle_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfle_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmflt_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmflt_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfne_vf_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
covergroup ExceptionsVf64_vmfne_vv_cg with function sample(ins_t ins);
    option.per_instance = 0;
    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_fs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.FS == Off (FP extension disabled). The cross pins mstatus.VS to
    // a non-Off value so the trap can only be attributed to FS being Off (not VS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins inactive = {0};
    }

    mstatus_vs_active_for_fs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_mstatus_fs_off : cross mstatus_fs_off, mstatus_vs_active_for_fs_off;

    //// end cp_mstatus_fs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_mstatus_vs_off
    // A vector floating-point instruction must raise an illegal-instruction trap
    // when mstatus.VS == Off (V extension disabled). The cross pins mstatus.FS to
    // a non-Off value so the trap can only be attributed to VS being Off (not FS).
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins inactive = {0};
    }

    mstatus_fs_active_for_vs_off : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins active = {[1:3]};
    }

    cp_mstatus_vs_off : cross mstatus_vs_off, mstatus_fs_active_for_vs_off;

    //// end cp_mstatus_vs_off////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state
    // Sample the vector-FP instruction under each non-Off mstatus.FS starting
    // state (Initial=1, Clean=2, Dirty=3) with mstatus.VS != Off. We cannot
    // observe the FS state transition directly (legal impls may hard-wire FS to
    // Dirty), so this coverpoint only ensures the test exercises every starting
    // FS state. Combined with signature-based cross-model checking, this catches
    // transitioning-model bugs.
    //////////////////////////////////////////////////////////////////////////////////

    mstatus_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "fs") {
        bins fs_initial = {1};
        bins fs_clean   = {2};
        bins fs_dirty   = {3};
    }

    mstatus_vs_active_for_fs_state : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "mstatus", "vs") {
        bins active = {[1:3]};
    }

    cp_vectorfp_mstatus_fs_state : cross mstatus_fs_state, mstatus_vs_active_for_fs_state;

    //// end cp_vectorfp_mstatus_fs_state////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_csr
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting CSR" case (e.g. vfdiv.vv 1.0/0.0 raising
    // the DZ flag in fflags) requires proving that mstatus.FS *transitioned*
    // to Dirty as a result of the vector-FP instruction modifying a fp CSR.
    // The RISC-V spec permits a legal always-Dirty implementation that
    // hard-wires mstatus.FS, so a coverage sample cannot distinguish "model
    // correctly transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_csr.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_csr////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_affecting_register
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "state-affecting register" case (e.g. vfmv.f.s) requires
    // proving that mstatus.FS *transitioned* to Dirty as a result of the
    // vector-FP instruction writing an architectural f-register. The RISC-V
    // spec permits a legal always-Dirty implementation that hard-wires
    // mstatus.FS, so a coverage sample cannot distinguish "model correctly
    // transitioned FS" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_affecting_register.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_affecting_register////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vectorfp_mstatus_fs_state_nonaffecting
    //
    // Intentionally empty: this coverpoint cannot be expressed in pure functional
    // coverage. The "nonaffecting" case (e.g. vfadd.vv with vs1_val == vs2_val
    // == 0) requires proving that mstatus.FS *did not* transition out of
    // Initial/Clean as a result of the vector-FP instruction. The RISC-V spec
    // permits a legal always-Dirty implementation that hard-wires mstatus.FS,
    // so a coverage sample of FS after the instruction cannot distinguish
    // "model correctly left FS unchanged" from "model is always-Dirty".
    //
    // The corresponding test sequence is still emitted by the priv test
    // generator (see generators/testgen/scripts/priv/cp_vectorfp_mstatus_fs_state_nonaffecting.py)
    // so that a transitioning model can be cross-checked against an always-Dirty
    // model via signature comparison.
    //////////////////////////////////////////////////////////////////////////////////

    //// end cp_vectorfp_mstatus_fs_state_nonaffecting////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vill
    //////////////////////////////////////////////////////////////////////////////////

    cp_vill: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins one  = {1};
    }

    //// end cp_vill////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart: coverpoint vstart_check(ins.hart, ins.issue) {
        bins vstart_one     = {vstart_one};
        bins vstart_vlmaxm1 = {vstart_vlmaxm1};
        bins vstart_vlmaxd2 = {vstart_vlmaxd2};
        bins vstart_legal   = {vstart_legal};   // random vstart
    }

    //// end cp_vstart////////////////////////////////////////////////

    //////////////////////////////////////////////////////////////////////////////////
    // cp_vstart_gt_vl
    //////////////////////////////////////////////////////////////////////////////////

    cp_vstart_gt_vl: coverpoint (get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") >  get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vl", "vl") &
                                 get_vtype_vlmax(ins.hart, ins.issue, `SAMPLE_BEFORE) > get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart")){
        bins true = {1};
    }

    //// end cp_vstart_gt_vl////////////////////////////////////////////////

endgroup
// ---------------------
function void exceptionsvf64_sample(int hart, int issue, ins_t ins);
    // Want to sample only if the SEW matches the target SEW of the file, some tests require
    // testing when vill is set, if vill=1 all other vtype bits are set to 0 so there is no
    // associated sew with these tests
    if (get_csr_val(hart, issue, `SAMPLE_BEFORE, "vtype", "vsew") == 3 ||
        get_csr_val(hart, issue, `SAMPLE_BEFORE, "vtype", "vill") == 1) begin
        case (traceDataQ[hart][issue][0].inst_name)
        "vfadd.vf"     : begin
            ExceptionsVf64_vfadd_vf_cg.sample(ins);
        end
        "vfadd.vv"     : begin
            ExceptionsVf64_vfadd_vv_cg.sample(ins);
        end
        "vfclass.v"     : begin
            ExceptionsVf64_vfclass_v_cg.sample(ins);
        end
        "vfcvt.f.x.v"     : begin
            ExceptionsVf64_vfcvt_f_x_v_cg.sample(ins);
        end
        "vfcvt.f.xu.v"     : begin
            ExceptionsVf64_vfcvt_f_xu_v_cg.sample(ins);
        end
        "vfcvt.rtz.x.f.v"     : begin
            ExceptionsVf64_vfcvt_rtz_x_f_v_cg.sample(ins);
        end
        "vfcvt.rtz.xu.f.v"     : begin
            ExceptionsVf64_vfcvt_rtz_xu_f_v_cg.sample(ins);
        end
        "vfcvt.x.f.v"     : begin
            ExceptionsVf64_vfcvt_x_f_v_cg.sample(ins);
        end
        "vfcvt.xu.f.v"     : begin
            ExceptionsVf64_vfcvt_xu_f_v_cg.sample(ins);
        end
        "vfdiv.vf"     : begin
            ExceptionsVf64_vfdiv_vf_cg.sample(ins);
        end
        "vfdiv.vv"     : begin
            ExceptionsVf64_vfdiv_vv_cg.sample(ins);
        end
        "vfmacc.vf"     : begin
            ExceptionsVf64_vfmacc_vf_cg.sample(ins);
        end
        "vfmacc.vv"     : begin
            ExceptionsVf64_vfmacc_vv_cg.sample(ins);
        end
        "vfmadd.vf"     : begin
            ExceptionsVf64_vfmadd_vf_cg.sample(ins);
        end
        "vfmadd.vv"     : begin
            ExceptionsVf64_vfmadd_vv_cg.sample(ins);
        end
        "vfmax.vf"     : begin
            ExceptionsVf64_vfmax_vf_cg.sample(ins);
        end
        "vfmax.vv"     : begin
            ExceptionsVf64_vfmax_vv_cg.sample(ins);
        end
        "vfmerge.vfm"     : begin
            ExceptionsVf64_vfmerge_vfm_cg.sample(ins);
        end
        "vfmin.vf"     : begin
            ExceptionsVf64_vfmin_vf_cg.sample(ins);
        end
        "vfmin.vv"     : begin
            ExceptionsVf64_vfmin_vv_cg.sample(ins);
        end
        "vfmsac.vf"     : begin
            ExceptionsVf64_vfmsac_vf_cg.sample(ins);
        end
        "vfmsac.vv"     : begin
            ExceptionsVf64_vfmsac_vv_cg.sample(ins);
        end
        "vfmsub.vf"     : begin
            ExceptionsVf64_vfmsub_vf_cg.sample(ins);
        end
        "vfmsub.vv"     : begin
            ExceptionsVf64_vfmsub_vv_cg.sample(ins);
        end
        "vfmul.vf"     : begin
            ExceptionsVf64_vfmul_vf_cg.sample(ins);
        end
        "vfmul.vv"     : begin
            ExceptionsVf64_vfmul_vv_cg.sample(ins);
        end
        "vfmv.f.s"     : begin
            ExceptionsVf64_vfmv_f_s_cg.sample(ins);
        end
        "vfmv.s.f"     : begin
            ExceptionsVf64_vfmv_s_f_cg.sample(ins);
        end
        "vfmv.v.f"     : begin
            ExceptionsVf64_vfmv_v_f_cg.sample(ins);
        end
        "vfnmacc.vf"     : begin
            ExceptionsVf64_vfnmacc_vf_cg.sample(ins);
        end
        "vfnmacc.vv"     : begin
            ExceptionsVf64_vfnmacc_vv_cg.sample(ins);
        end
        "vfnmadd.vf"     : begin
            ExceptionsVf64_vfnmadd_vf_cg.sample(ins);
        end
        "vfnmadd.vv"     : begin
            ExceptionsVf64_vfnmadd_vv_cg.sample(ins);
        end
        "vfnmsac.vf"     : begin
            ExceptionsVf64_vfnmsac_vf_cg.sample(ins);
        end
        "vfnmsac.vv"     : begin
            ExceptionsVf64_vfnmsac_vv_cg.sample(ins);
        end
        "vfnmsub.vf"     : begin
            ExceptionsVf64_vfnmsub_vf_cg.sample(ins);
        end
        "vfnmsub.vv"     : begin
            ExceptionsVf64_vfnmsub_vv_cg.sample(ins);
        end
        "vfrdiv.vf"     : begin
            ExceptionsVf64_vfrdiv_vf_cg.sample(ins);
        end
        "vfrec7.v"     : begin
            ExceptionsVf64_vfrec7_v_cg.sample(ins);
        end
        "vfredmax.vs"     : begin
            ExceptionsVf64_vfredmax_vs_cg.sample(ins);
        end
        "vfredmin.vs"     : begin
            ExceptionsVf64_vfredmin_vs_cg.sample(ins);
        end
        "vfredosum.vs"     : begin
            ExceptionsVf64_vfredosum_vs_cg.sample(ins);
        end
        "vfredusum.vs"     : begin
            ExceptionsVf64_vfredusum_vs_cg.sample(ins);
        end
        "vfrsqrt7.v"     : begin
            ExceptionsVf64_vfrsqrt7_v_cg.sample(ins);
        end
        "vfrsub.vf"     : begin
            ExceptionsVf64_vfrsub_vf_cg.sample(ins);
        end
        "vfsgnj.vf"     : begin
            ExceptionsVf64_vfsgnj_vf_cg.sample(ins);
        end
        "vfsgnj.vv"     : begin
            ExceptionsVf64_vfsgnj_vv_cg.sample(ins);
        end
        "vfsgnjn.vf"     : begin
            ExceptionsVf64_vfsgnjn_vf_cg.sample(ins);
        end
        "vfsgnjn.vv"     : begin
            ExceptionsVf64_vfsgnjn_vv_cg.sample(ins);
        end
        "vfsgnjx.vf"     : begin
            ExceptionsVf64_vfsgnjx_vf_cg.sample(ins);
        end
        "vfsgnjx.vv"     : begin
            ExceptionsVf64_vfsgnjx_vv_cg.sample(ins);
        end
        "vfslide1down.vf"     : begin
            ExceptionsVf64_vfslide1down_vf_cg.sample(ins);
        end
        "vfslide1up.vf"     : begin
            ExceptionsVf64_vfslide1up_vf_cg.sample(ins);
        end
        "vfsqrt.v"     : begin
            ExceptionsVf64_vfsqrt_v_cg.sample(ins);
        end
        "vfsub.vf"     : begin
            ExceptionsVf64_vfsub_vf_cg.sample(ins);
        end
        "vfsub.vv"     : begin
            ExceptionsVf64_vfsub_vv_cg.sample(ins);
        end
        "vmfeq.vf"     : begin
            ExceptionsVf64_vmfeq_vf_cg.sample(ins);
        end
        "vmfeq.vv"     : begin
            ExceptionsVf64_vmfeq_vv_cg.sample(ins);
        end
        "vmfge.vf"     : begin
            ExceptionsVf64_vmfge_vf_cg.sample(ins);
        end
        "vmfgt.vf"     : begin
            ExceptionsVf64_vmfgt_vf_cg.sample(ins);
        end
        "vmfle.vf"     : begin
            ExceptionsVf64_vmfle_vf_cg.sample(ins);
        end
        "vmfle.vv"     : begin
            ExceptionsVf64_vmfle_vv_cg.sample(ins);
        end
        "vmflt.vf"     : begin
            ExceptionsVf64_vmflt_vf_cg.sample(ins);
        end
        "vmflt.vv"     : begin
            ExceptionsVf64_vmflt_vv_cg.sample(ins);
        end
        "vmfne.vf"     : begin
            ExceptionsVf64_vmfne_vf_cg.sample(ins);
        end
        "vmfne.vv"     : begin
            ExceptionsVf64_vmfne_vv_cg.sample(ins);
        end
        endcase
    end
endfunction
